from plptest import *

TestConfig = c = {}

test = Test(
  name = 'occupancy_management',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'cd ../ && make && cd - && make all'),
    Shell('run',   'make run')
  ],
  timeout=1000000
)

c['tests'] = [ test ]
