from plptest import *

TestConfig = c = {}

test = Test(
  name = 'occupancy_management',
  commands = [
    Shell('clean', 'make clean'),
    Shell('shutterless', 'cd ../ && make && cd -'),
    Shell('build', 'make all'),
    Shell('run',   'make run')
  ],
  timeout=1000000
)

c['tests'] = [ test ]
